describe("getRole", () => {
  test("Should return a string", () => {
    const expected = "";
  });
}),
  describe("getGitHub", () => {
    test("Should return a string ", () => {
      const expected = "";
    });
  });
