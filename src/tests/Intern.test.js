describe("getRole", () => {
  test("Should return a string", () => {
    const expected = "";
  });
});

describe("getSchool", () => {
  test("Should return a string", () => {
    const expected = "";
  });
});
