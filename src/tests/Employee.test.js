const { test } = require("picomatch");

describe("getName", () => {
  test("Should return a string ", () => {
    const expected = "";
  });
}),
  describe("getId", () => {
    test("Should return a string", () => {
      const expected = "";
    });
  }),
  describe("getEmail", () => {
    test("Should return a string", () => {
      const expected = "";
    });
  }),
  describe("getRole", () => {
    test("Should return a string", () => {
      const expected = "";
    });
  });
