const inquirer = require("inquirer");

const writeToFile()

const generateHTML() => {
    map
    // getRole 
    
}

answers = {
    teamname: "",
    employees: [
        name:
        email:
        id:
        role: 
    ]
}